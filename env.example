# EDITORIAL OS LEDGER - ENVIRONMENT CONFIGURATION
# Copy this to .env.local and fill in your actual values

# =============================================================================
# SUPABASE CONFIGURATION (Required)
# =============================================================================
# Get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: Supabase anon key (if you need client-side access)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# =============================================================================
# EDITORIAL OS CONFIGURATION
# =============================================================================
EDITORIAL_OS_VERSION=3.0.0
MCP_PROTOCOL_VERSION=1.0.0

# Service identifier (for debugging/logging)
SERVICE_NAME=editorial-os-ledger
SERVICE_INSTANCE=production

# =============================================================================
# EXTERNAL TOOL INTEGRATIONS (Optional - for Level 4-5)
# =============================================================================

# Beehiiv Integration (Newsletter Agent)
BEEHIIV_API_KEY=your-beehiiv-api-key
BEEHIIV_PUBLICATION_ID=your-publication-id

# Buffer Integration (Social Engine)
BUFFER_ACCESS_TOKEN=your-buffer-access-token
BUFFER_PROFILE_IDS=comma,separated,profile,ids

# Notion Integration (Custom Client Integration)
NOTION_API_KEY=your-notion-integration-key
NOTION_DATABASE_ID=your-database-id

# Slack Integration (Notifications)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url
SLACK_BOT_TOKEN=xoxb-your-bot-token

# =============================================================================
# SECURITY & AUTHENTICATION (Production)
# =============================================================================

# API Keys for MCP access (if you want to secure the endpoints)
MCP_API_KEY=your-secret-mcp-api-key
ORCHESTRATOR_API_KEY=your-chat-orchestrator-key

# JWT Secret (if implementing authentication)
JWT_SECRET=your-super-secret-jwt-key

# Allowed origins (CORS)
ALLOWED_ORIGINS=https://your-chat-orchestrator.vercel.app,https://your-domain.com

# =============================================================================
# LOGGING & MONITORING (Optional)
# =============================================================================

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# External logging service (optional)
LOGTAIL_TOKEN=your-logtail-token
SENTRY_DSN=your-sentry-dsn

# =============================================================================
# PERFORMANCE & OPTIMIZATION
# =============================================================================

# Database connection settings
DB_CONNECTION_TIMEOUT=30000
DB_STATEMENT_TIMEOUT=60000

# Cache settings (if implementing Redis later)
REDIS_URL=redis://localhost:6379
CACHE_TTL=300

# Rate limiting (requests per minute per IP)
RATE_LIMIT_RPM=100

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================

# Enable debug logging in development
DEBUG=false

# Mock external services (for testing)
MOCK_BEEHIIV=false
MOCK_BUFFER=false
MOCK_NOTION=false

# Test database (separate from production)
TEST_SUPABASE_URL=https://your-test-project.supabase.co
TEST_SUPABASE_SERVICE_KEY=your-test-service-key

# =============================================================================
# CLAUDE CODE / MCP INTEGRATION
# =============================================================================

# If using Claude Code agents, specify the agent names that can access this ledger
ALLOWED_AGENTS=brief-specialist,newsletter-agent,social-engine,dam-agent

# MCP server registration (for Claude Code)
MCP_SERVER_NAME=editorial-os-ledger
MCP_SERVER_VERSION=3.0.0

# =============================================================================
# VERCEL DEPLOYMENT SETTINGS
# =============================================================================

# These are set automatically by Vercel, but you can override if needed
# VERCEL=1
# VERCEL_ENV=production
# VERCEL_URL=your-deployment-url.vercel.app

# =============================================================================
# BACKUP & DATA RETENTION (Production)
# =============================================================================

# How long to keep campaign data (days)
DATA_RETENTION_DAYS=365

# How long to keep event logs (days) 
EVENT_LOG_RETENTION_DAYS=90

# How long to keep metrics (days)
METRICS_RETENTION_DAYS=730

# Backup settings (if implementing automated backups)
BACKUP_ENABLED=true
BACKUP_FREQUENCY=daily
BACKUP_S3_BUCKET=your-backup-bucket

# =============================================================================
# INSTRUCTIONS
# =============================================================================
# 
# 1. Copy this file to .env.local
# 2. Fill in the REQUIRED values (Supabase URL and Service Role Key)
# 3. Add any optional integrations you want to use
# 4. Deploy to Vercel (environment variables will be set automatically)
#
# For development:
#   - Set DEBUG=true for verbose logging
#   - Use TEST_ variables for separate test database
#   - Set MOCK_* to true to avoid calling real external services
#
# For production:
#   - Set strong JWT_SECRET and MCP_API_KEY values
#   - Configure ALLOWED_ORIGINS for security
#   - Set up monitoring with SENTRY_DSN or LOGTAIL_TOKEN
#   - Enable backups in production
#